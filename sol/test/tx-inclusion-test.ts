

describe('RelayContract', async () => {
    it("Should test all tx inclusion verification submissions to an ark based proof", async () => {

    });

    it("Should test all tx inclusion verification submissions to a bellman based proof", async () => {

    });
});